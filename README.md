# Smart Library System

---

## 📘 Overview
The **Smart Library System** is a Python project that implements an **object-oriented library management system**.  
It manages a collection of books, supports efficient searching, and generates book recommendations.  
The project compares **linear search** and **binary search** performance and includes optional features such as borrowing and user history–based recommendations.

---

## 🧠 System Summary
The system is built using three main classes:

- **Book** – stores title, author, genre, year, and rating  
- **Library** – manages books, searching, recommendations, and reports  
- **User** – tracks borrowed books and borrowing history  

### Core Features
- Linear search (O(n)) and binary search (O(log n))
- Top-rated and genre-based recommendations
- Performance timing and comparison
- Optional borrowing and returning functionality
- User history–based recommendations

---

## ▶️ How to Run
Open the `main.ipynb` file using Jupyter Notebook, JupyterLab, or VS Code.  
Run cells sequentially using **Shift + Enter**.
